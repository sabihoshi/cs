using System;

namespace Ternary
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            int grade = 50;
            Console.WriteLine(grade >= 75 ? "Pass" : "Fail");
            Console.ReadKey();

            // Go to http://aka.ms/dotnet-get-started-console to continue learning how to build a console app!
        }
    }
}